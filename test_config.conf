# 🚀 Parallel Compaction 테스트 설정 파일
# 이 파일을 수정하여 실험 파라미터를 조정할 수 있습니다.

# ========================================
# 기본 경로 설정
# ========================================
ROCKSDB_PATH="./"
DB_BENCH="./db_bench"

# ========================================
# 실험 데이터 설정  
# ========================================
# 테스트할 키 개수 (메모리와 시간을 고려하여 조정)
NUM_KEYS=50000000     # 5천만 (약 5GB, 권장)
# NUM_KEYS=10000000   # 1천만 (약 1GB, 빠른 테스트용)
# NUM_KEYS=100000000  # 1억 (약 10GB, 심화 분석용)

VALUE_SIZE=100        # 값 크기 (bytes)
KEY_SIZE=16          # 키 크기 (bytes)

# ========================================
# RocksDB 설정
# ========================================
WRITE_BUFFER_SIZE="64MB"
MAX_WRITE_BUFFER_NUMBER=8
TARGET_FILE_SIZE_BASE="64MB"
CACHE_SIZE="1073741824"  # 1GB
COMPRESSION_TYPE="snappy"

# ========================================
# 테스트 매개변수
# ========================================
# 테스트할 subcompaction 값들 (공백으로 구분)
SUBCOMPACTION_VALUES="1 2 4 8 12 16 24 32"

# 빠른 테스트용 (시간 절약)
# SUBCOMPACTION_VALUES="1 4 8 16"

# 심화 분석용
# SUBCOMPACTION_VALUES="1 2 3 4 6 8 10 12 16 20 24 28 32"

MAX_BACKGROUND_JOBS=16

# ========================================
# 모니터링 설정
# ========================================
MONITOR_INTERVAL=1      # 시스템 모니터링 간격 (초)
REPORT_INTERVAL=10      # db_bench 리포트 간격 (초)

# ========================================
# 결과 저장 설정
# ========================================
# 결과 디렉토리 (자동 생성됨)
RESULTS_BASE_DIR="./results"
KEEP_DB_FILES=false     # true: DB 파일 보관, false: 정리

# ========================================
# 고급 설정
# ========================================
# 테스트 간 대기 시간 (시스템 안정화)
STABILIZATION_WAIT=30   # 초

# 병렬 읽기 테스트 스레드 수
READ_TEST_THREADS=16

# 실험 반복 횟수 (통계적 신뢰성 향상)
REPEAT_COUNT=1          # 1: 단일 실행, 3: 3회 평균

# ========================================
# 시스템 요구사항 (참고용)
# ========================================
# 권장 최소 사양:
# - CPU: 4코어 이상
# - Memory: 8GB 이상 
# - Storage: 20GB 이상 여유 공간
# - OS: Linux (Ubuntu 18.04+, CentOS 7+ 권장)

# 최적 사양:
# - CPU: 16코어 이상
# - Memory: 32GB 이상
# - Storage: NVMe SSD 50GB 이상 